"use strict";
// // Dependencies:
// import * as fs from 'fs';
// import * as path from 'path';
// import { createPrinter, SourceFile } from 'typescript';
// import { tsquery } from '../src/index';
//
// // Queries:
// const NG_MODULE_IMPORTS = `Decorator > CallExpression:has([expression.name="NgModule"]) > ObjectLiteralExpression > PropertyAssignment[name.name="imports"] > ArrayLiteralExpression`;
//
// const ngModule = fs.readFileSync(path.join(__dirname, './templates/angular.module.ts'), 'utf8');
// const ast = tsquery.ast(ngModule);
//
// const printer = createPrinter();
//
// const [imports] = tsquery(ast, NG_MODULE_IMPORTS);
// let [newImport] = tsquery(tsquery.ast('MyCoolModule'), 'Identifier');
// newImport.pos = -1;
// newImport.end = -1;
// (imports.elements as any).push(newImport);
// console.log(imports.elements);
// console.log(printer.printFile(ast as SourceFile));
// debugger;
//# sourceMappingURL=angular.js.map