"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
// Dependencies:
var match_1 = require("../match");
var traverse_1 = require("../traverse");
function has(node, selector) {
    var collector = [];
    selector.selectors.forEach(function (childSelector) {
        var ancestors = [];
        traverse_1.traverse(node, {
            enter: function (childNode, parentNode) {
                if (parentNode != null) {
                    ancestors.unshift(parentNode);
                }
                if (match_1.findMatches(childNode, childSelector, ancestors)) {
                    collector.push(childNode);
                }
            },
            leave: function () {
                ancestors.shift();
            }
        });
    });
    return collector.length > 0;
}
exports.has = has;
//# sourceMappingURL=has.js.map