import {MaskCasePipe} from './mask-case.pipe';

describe('MaskCasePipe', () => {
    it('create an instance', () => {
        const pipe = new MaskCasePipe();

        expect(pipe).toBeTruthy();
    });
});
