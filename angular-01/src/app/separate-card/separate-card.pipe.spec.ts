import {SeparateCardPipe} from './separate-card.pipe';

describe('SeparateCardPipe', () => {
    it('create an instance', () => {
        const pipe = new SeparateCardPipe();

        expect(pipe).toBeTruthy();
    });
});
